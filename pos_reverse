a= [int(i) for i in input("Enter Strings:").split()]
#a=[7,9,4,-6,-1,2]
def pos_sort(a):
    for i in range(len(a)):
        if a[i] <= 0:
            continue
        else :
            for j in range(i,len(a)):
                if a[j] <=0:
                    continue
                elif a[i] > a[j]:
                    a[i],a[j]=a[j],a[i]
    print(a)

pos_sort(a)


